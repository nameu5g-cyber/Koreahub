---
description: Мгновенная проверка целостности проекта и запуск тестового сервера (localhost:3000)
---

// turbo-all

Этот воркфлоу автоматизирует "подъем" тестовой среды KoreaHub.

### Шаги:
1. **Проверка окружения**:
   - Убедиться, что файл `.env.local` существует.
   - Проверить наличие `GEMINI_API_KEY`.
2. **Проверка зависимостей**:
   - Убедиться, что папка `node_modules` существует. Если нет — вызвать `npm install` (через cmd).
3. **Статус порта**:
   - Проверить, занят ли порт 3000 (`netstat -ano | findstr :3000`).
4. **Запуск**:
   - Если порт свободен, запустить сервер: `cmd /c "npm run dev"`.
   - Если порт занят, просто вывести: "🚀 Сервер уже запущен на http://localhost:3000".

### Пример использования:
Просто напишите `/up` в чате.
